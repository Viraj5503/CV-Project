# Core CV stack (Step 2-3)
ultralytics>=8.3.0        # YOLOv11 training/inference
torch>=2.5.0              # backend (mps on Apple Silicon)

# Data prep + EDA (Step 1)
pillow>=10.0.0
pyyaml>=6.0
matplotlib>=3.8.0

# VLM inspection reports (Step 3)
anthropic>=0.40.0
python-dotenv>=1.0.0      # loads .env (ANTHROPIC_API_KEY, PCB_VLM_MODEL)

# Serving (Step 4)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9   # file uploads in FastAPI
streamlit>=1.40.0
requests>=2.32.0

# RAG repair advisor (Phase 2, Task 1)
chromadb>=1.0.0           # persistent vector store (data/chroma)
sentence-transformers>=3.0.0  # local embeddings (all-MiniLM-L6-v2)
langchain-text-splitters>=0.3.0  # header-aware markdown chunking

# RAG generation providers (Phase 2, Task 2)
google-genai>=1.0.0       # Gemini free-tier generation (GEMINI_API_KEY)

# Tests
pytest>=8.0.0
httpx>=0.27.0             # FastAPI TestClient transport
